export const selectProductsData = (state) => state.products.products;
export const selectProductsDetails = (state) => state.products.productDetails;
export const selectLocations = (state) => state.products.locations;
